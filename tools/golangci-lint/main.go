// golangci-lint is a wrapper around golangci-lint that downloads the binary and
// runs it with the given arguments.
//
// It is not implemented yet but could be used in the future to avoid
// dependencies on the host system. Its implementation could be similar to
// tools/protoc's implementation but since golangci-lint is run way more often
// than protoc, the binary should be cached.
package main

func main() {
	panic("not implemented")
}
